[project]
name = "gitee-mcp"
version = "0.2.0"
description = "Gitee (Chinese GitHub) bridge - trending repo radar, search, repo intel and zh to en translation"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=3.4.4,<4",
    "prefab-ui>=0.14.0",
    "fastapi>=0.115,<1",
    "uvicorn>=0.30",
    "httpx>=0.27",
    "beautifulsoup4>=4.12",
    "pydantic>=2.7",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "pytest-cov>=5.0",
    "pre-commit>=3.5",
    "respx>=0.21",
    "ruff>=0.5",
    "pyright>=1.1.370",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/gitee_mcp"]

[tool.ruff]
line-length = 100
target-version = "py311"
extend-exclude = [".venv", "webapp", "mcpb", "docs", "assets"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP", "SIM", "T20"]
ignore = ["E501"]
per-file-ignores = { "scripts/*.py" = ["T20"] }

[tool.ruff.format]
quote-style = "double"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = "--cov=gitee_mcp --cov-report=term-missing --cov-fail-under=60"

[tool.pyright]
include = ["src"]
exclude = ["webapp", "mcpb", ".venv"]
pythonVersion = "3.11"
